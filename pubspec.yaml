name: fordev
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.1.0
  faker: ^2.0.0
  build_runner: ^2.1.7

flutter:
  uses-material-design: true
  assets:
    - lib/ui/assets/
